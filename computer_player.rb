#COMPUTER PLAYER IMPLEMENTATION
require 'Matrix'
#algorithm? 

class ComputerPlayer
    def move(board, player)
        #returns move choice for a given state of the board
        if "First Move"
            #makes opening move
        elsif "possible to win"
            #wins
        elsif "possible to block"
            #blocks
        else
            #random move?
        end
    end

    def win?(board, player)
        #checks if possible for computer to win
    end

    def block?(board, player)
        #checks if necessary to block
    end


end